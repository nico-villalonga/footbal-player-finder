jest.mock('../api');

// import appi from '../api';
// import players from '../../config/mockData';

describe('test api', () => {
    it('should call fetchPlayers', () => {
    //     expect.assertions(1);
    //     return api.fetchPlayers()
    //         .then(data => expect(data).toEqual(players));
    // });

    // it('should call fetchMockPlayers', () => {
    //     expect.assertions(1);
    //     return api.fetchMockPlayers()
    //         .then(data => expect(data).toEqual(players));

        expect(1).toBe(1);
    });

});
